```markdown
# GDFlix TMDB Poster Bot (Modular)

Refactor of original single-file bot into modules to be GitHub-friendly and ready for deployment.

- Put your secrets in `.env` or platform environment variables.
- Install system dependency `mediainfo` (required by mediainfo module).
- Run `python -m bot.run` to start.

See `requirements.txt` and `Procfile` for deployment hints.
```